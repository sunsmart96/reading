# 重构，改善既有代码的设计

经典老书的。对于一些人认为手写代码就是古法编程，那么这本书没什么意义的。可是我想说，Ai编程也不是只是对着对话框写一些人类语言。真实的工程项目中，需要加入大量约束和工具甚至自定义agent才行。当前不论是国外的claude code/codex/gemini还是国内的glm/kimi，他们当前设计的agent的水平还远远没有到用户直接输入一串自然语言就能生成真正满足工程约束的高质量工业级代码能力。这不是否定AI coding，而是要基于当下的技术水平的清晰认识。这是设计未来新一代可以超越当前claude code/codex的agent的前提。站在这个角度这本书依然存在很大的价值。

我经常用codex写代码，相比于不用AI辅助，使用AI的确在效率上有很大的提升，更重要的是AI可以教会我很多我不会的知识。这比我之前在公司中所谓的mentor强太多太多了。在假定基础模型条件不变的情况下，如果研究AI coding中agent的行为效果，基于我的实操经验，我发现执行coding任务的准确度/时间仍然存在巨大提升。人类才过去50年积累的软件工程实践经验还存在大量没有被agent化的内容。这就是我当前最关心的技术方向之一，这里可以做出世界影响力的开源项目，或者不错的创业机会或者在公司内部打造起具备世界领先技术能力的软件体系。

李飞飞的博士生Andrej Karpathy领导了特斯拉fsd的核心研发，回到斯坦福后经常分享一些vibe coding实践。比如skills的技巧: [https://github.com/forrestchang/andrej-karpathy-skills](https://github.com/forrestchang/andrej-karpathy-skills)

```markdown
---
name: karpathy-guidelines
description: Behavioral guidelines to reduce common LLM coding mistakes. Use when writing, reviewing, or refactoring code to avoid overcomplication, make surgical changes, surface assumptions, and define verifiable success criteria.
license: MIT
---

# Karpathy Guidelines

Behavioral guidelines to reduce common LLM coding mistakes, derived from [Andrej Karpathy's observations](https://x.com/karpathy/status/2015883857489522876) on LLM coding pitfalls.

**Tradeoff:** These guidelines bias toward caution over speed. For trivial tasks, use judgment.

## 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them - don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

## 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it - don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

The test: Every changed line should trace directly to the user's request.

## 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.
```

但是我觉得这还不够，modern software engineering的变革才刚刚开始而已。把历史上的软件工程经验转变为更高效率更可靠更稳定的agent是未来两年非常有前景的事情。

现代软件工程的现代体现在让AI实现，并不是否定过去50年软件工程经验本身。

对于代码重构，不论是人写还是AI写，从来都没有一次性写完就万事大吉的事情，都是需要伴随着开发，不断迭代重构的。

# 0代码重构的案例

有一个软件项目，用java写的，了解java语言特性的都知道，实现hello world都要写个类。稍微大一点的真实项目都是一堆继承关系。软件顾问检查代码发现系统相当凌乱，上层超类对系统做了一些假设，下层子类实现了这些假设。但是这些假设并不适用于所有的类，导致覆写的工作量非常大。超类的一些意图并未被子类理解。因此存在大量代码重复。顾问看了代码建议项目经理对代码进行重构，把代码好好整理一下，但是项目经理并不热衷于此，毕竟程序看上去可以运行，而且项目面临很大的压力。顾问把相同的代码问题反馈给程序员，程序员是懂软件的，明白这些问题后续会导致更多的问题，所以花时间把代码做了重构。项目经理知道后非常不满意，项目有很多工作需要做，时间很紧，系统就要在几个月后发布，在这种情况下，程序员花费时间做与功能交付无关的东西。原来的代码跑起来也算正常。顾问建议软件其他部分也进行这样的重构，但是项目经理觉得是过度设计过于追求完美。六个月后，项目失败，由于软件设计的太过复杂，难以调试，更难以实现相应的功能。后来项目重启，几乎重写，按照之前的重构方法，不断完善内部模块，最后重新立项的项目获得通过。

重构是在不改变代码外在行为的条件下，对代码内部进行修改。一般的软件开发会在初期进行设计，设计完进行编码，在产品迭代过程中，由于不断加入新的需求，以至于最开始的设计已经不再适合当前的系统需求，这时就需要进行重构。重构是伴随软件开发不断重复的过程。不幸的是我在现实中遇到的项目经理真的几乎所有都是跟上面的一样。因为至少在中国，真正懂软件的项目经理凤毛麟角。成天干的就是拉通对齐，对于项目真正的内在并没有足够的技术判断力和掌控力。在项目开发过程中也从来没有专门花时间去优化代码结构，都是按照功能列表去开发功能。事实上这本身就是不科学的。

上面的例子中是一个实际的案例，说明了在软件开发过程中重构的重要性。实际的情况没有标准答案，什么时候重构，是否需要重构跟产品的生命周期定位紧密结合。根据项目实际情况综合判断的，不过根据我的经验，代码重构最好以月为单位，每个月抽出几天把之前因为赶工期造成的混乱的代码整理一下比较好，否则时间长了再改，工作量和难度以及影响都是翻倍的增加。

重构的好处包括：改进软件设计，毕竟没有任何软件可以在一开始阶段就把后续所有事情想清楚；使软件更容易理解，即使目前有AI辅助，也需要人去查看代码把握代码；帮助找到bug，代码结构太乱实际上不方便调试排查问题，我前段时间接受了一个项目，号称是老程序员写的，结果各种耦合/魔法数字/内存错误，在上面加点功能或者调试老费劲了；提高开发速度，只有良好的软件架构设计和代码设计，才更容易更改代码提高开发效率，现在的AI就很容易写屎山，一旦出了问题改起来非常费劲。

兵法上说不谋全局者不足谋一域，不谋万世者不足谋一时。这在重构中也是一样的。如果仅仅为了达成当前的目标而不择手段，破坏了体系化的结构，等到明天再来新的需求，就一定是一团糟。在实际开发中我见过太多这样的事情了。

# 1代码的坏味道

什么样的代码是不好的，应该被重构掉？怎么样识别不好的代码。这对于设计高层次的AI Agent是非常重要的

不好的代码存在的一些特征问题

重复的代码

过长的函数

过大的类

过长的参数列

发散式变化：类的定位设计发散，导致引入新的变化需要更改多个函数

霰弹式修改：一个很小的需求，需要多个类的多个函数都要进行小修改

依恋情结：某个类的方法代码中绝大多数内容都是修改其他某个类的数据，这意味着设计存在偏差

switch惊悚：面向对象的程序中尽量少用switch，从语义上讲，switch可以通过设计类继承的多态替换掉，毕竟switch就会有一堆分支，代码越积累越多，就不好改和维护了

冗余类：没有发挥作用的类就删了，这并不意味着一些抽象类或者作为类型的底层基础类没有意义

令人迷惑的暂时字段：如果类中的一些数据成员是在某种情况下才使用，可以把这些成员单独出去一个类，设计对应的方法来使用

狎昵关系：两个类关系过于紧密，甚至没有清晰界限，可以考虑合并

异曲同工的类：干的一样的事情却设计成两个类，可以合并开不同接口

被拒绝的馈赠：如果子类想继承父类的一些行为但是又不想支持父类的接口，可以考虑把继承关系改为代理模式

过多的注释：适当的注释是必要的，但是没必要长篇大论

# 2构筑测试体系

重构的前提是需要有可靠的测试环境。我前段时间就接了一个项目，甲方连测试数据都不给，没有任何单元测试，就要重构原来的屎山代码。甚至连功能都说不清楚。

书里面用的自动化测试框架是JUnit，这个根据语言和背景不同可以自行选择。JUnit是用于单元测试的。除了单元测试，还有功能测试/集成测试。单元测试是高度局部化的。功能测试则完全不同，它们是用来保证软件正常运行，他们从客户角度保障质量，并不关心程序员的生产力。单元测试一般是开发团队自己做，功能测试一般交给喜欢寻找bug的独立团队来开发。功能测试一般把整个系统当作一个黑箱，去验证软件产品对外提供的预期功能。如果发现了bug，最好在bug发生的地方添加单元测试。这样才能逐步构建起稳定的软件产品。

# 3一些常用的代码重构方法和准则

## 3.1重新组织函数

提炼函数：是把原来的函数按照设计目标进行分解，把庞大的函数分解为一个个相对独立，逻辑完整的函数，进而降低单个函数过于庞大复杂难以更改的问题。而且这样做可以更早的从代码设计角度考虑问题，避免堆积屎山

内联函数：在函数调用点移除函数调用插入本体。这个原则不是绝对的，只是针对一些啰嗦的写法的优化

内联临时变量：如果一个变量只被简单表达式赋值一次，可以将对该变量的引用动作替换为对它赋值的那个表达式本身

引入解释型变量：将复杂表达式的一部分放进临时变量，增加可读性，否则太长的调用链如果出现bug不好调试

分解临时变量：如果一个临时变量被赋值多次，而且既不是循环变量也不是用于搜集计算结果，针对每次赋值单独创建一个临时变量。之前有一个项目是做CFD仿真，全是数值计算，代码写的非常不规范，一个变量在很长的函数中到处赋值，根本不好把握到底在干什么，导致非常难以调试

函数对象取代函数：如果一个函数包含大量计算过程，可以考虑把里面的变量封装成类和对外提供接口，在新的类中用更多短小的成员函数来分解巨大的函数

## 3.2在对象之间搬移特性

搬移函数：如果一个类中的的方法频繁调用另外类的方法甚至比调用自身类方法还多，可以考虑把该方法移动到其他类中。这种重构的方法需要考虑方法对自身类的依赖程度。因为如果一个类的方法太多，实际上就高度耦合了，这样的设计在大型软件中很难维护和调试

搬移字段：如果一个类中的成员更多的被另外一个类调用或者使用，可以考虑把字段迁移到外部类中。思路和方法跟搬移函数差不多，需要考虑耦合依赖关系

提炼类：如果一个类做了两个类的事情，建立一个新类，把相应字段和函数从旧的搬移到新类中。这块设计对业务的理解和抽象的问题，对于业务理解深入才能更好的有遇见的进行抽象，才能更好的设计类，设计抽象层次

将类内联化：如果一个类没做太多的事情，可以考虑将其迁移到其他类中，移除原有类

隐藏委托关系：客户通过委托类来调用另外一个对象。在面向对象编程中封装是最关键的特征之一。封装意味着调用者对系统了解更少，那么对于系统变化受到的影响也就越小，这样才能降低系统变更之间的影响

移除中间人：隐藏委托关系是通过加一个中间层来隔离显式依赖关系，但是如果中间层过多，整个体系也会显得臃肿。到底什么时候是应该加中间层，什么时候应该减少中间层其实没有明确的数值标准。所以软件的架构设计不完全是工程也是一种艺术的体现

引入外加函数：当一个提供服务的类已经设计的非常好，但是还想给它加一个方法，但是从架构设计角度不应该在类中加方法，可以考虑在调用方加一个函数，并且把这个类作为函数的第一个参数，在上面加一个wrapper

## 3.3重新组织数据

自封装字段：为字段设置取值和设值函数，并只用这些函数访问字段。这个不绝对，不过对于非只包含字段的类，由于封装性最好这样做，否则代码多了不好管理代码

以对象取代数据值：如果有一个数据值需要跟其他数据一起使用才能有意义，可以把这些数据封装带一个类中

将值对象改为引用对象：值对象意味着每个都是单独的备份，如果是被多个其他对象引用，都是用值对象一个问题是比较占用空间，而且数据同步也不好控制

将引用对象改为值对象：一般情况下对于复杂对象都是以引用为主，对于简单对象且没有被其他多个对象共同使用的，可以考虑直接使用值对象

以对象取代数组：比如一个表示属性的类，里面的成员都是字符串，当然可以使用数组来存储，但是不同的索引代表不同的字段本身就不好维护，如果替换为对象中的各种成员，语义就很很清晰

复制被监测数据：比如在gui软件开发中，除了gui还有数据处理部分。gui是非常容易改变的，相对而言数据部分变化没有那么频繁，且一般开发交互和开发后台数据会分在两个不同的团队中。如果使用常规类似于qt widget默认模式那样把gui和业务逻辑处理都放在一起，就很难分开团队做。目前不论什么技术栈，都在做前后端分离。这样的好处就是变更前端，后端的逻辑不用频繁改动，可以分成前后端两个团队独立开发，只要双方按照约定好的接口就行

以字面值常量取代魔法数字：如果一个字面值具备特别意义，创建一个常量，根据它的意义命名，并将上述的字面值替换这个常量。随便写魔法数字别人根本不知道什么意思

封装集合：类的成员包含集合这种成员的时候，如果某个方法直接返回这个变量，那么外部的对象就可以绕过集合的所有者更改集合。所以书中推荐返回不可变的集合只读副本，并且在拥有集合的类中添加给集合增删改查的接口。其实如果是返回只读副本不如直接把对集合的操作结果返回出来，集合不会以任何方式离开这个类

以类型代替类型码：这里说的类型码就是类似于C/C++中的各种用整形数字表示的编号代码。因为这种作为数字的基础类型没有跟业务相关的检查机制，所以在大型项目中容易出现逻辑混乱不方便调试。C++现代的版本提供了enum class是一个比较好的解决方案。这本书使用的很很老版本的java进行说明的，具体内容上存在过时问题

以子类型代替类型码：书里面使用的还是很古老的java语法了，不过一般来讲对于C++/JAVA这种静态类型语言，静态类型的特点就是可以尽可能把一些类型错误在编译期就检查出来，避免拖到运行期再爆bug。所以把C语言中那种用int表述的类型码换成子类型，就可以伴随业务的复杂，加入更多的功能，而且可以直接用编译器在编译期检查类型错误

以state或strategy取代类型码：类型码还是上面说的那些，之所以开始使用设计模式，是因为在这种情况下无法通过继承方式消除类型码

以字段取代子类：如果子类之间唯一的差别就是返回常量不同，可以把这些字段集成到父类中作为成员并销毁子类

## 3.4简化条件表达

分解条件表达式：当有一个复杂的条件判断表达式的时候，可以把条件判断单独成一个函数

合并条件表达式：有一系列测试返回相同的结果，可以把这些测试合并为一个条件表达式并提炼为一个函数，这样对于业务代码维护可读性比较好

合并重复的条件片段：在条件表达式的每个分支上有相同的一段代码，可以把这段代码移到表达式外面，因为这部分代码放在条件里面是重复冗余的代码

移除控制标记：在一系列布尔表达式中，某个变量带有控制标记的作用，以break语句或者return语句代替控制标记。我在最开始写代码的时候就经常用一个bool量来做控制标记。判断完一个情况后再去改这个bool值。可读性的确差一些，用break或者return就很明确表明这个查询结束了，而手动改bool值没有这么清晰的语义

以卫语句代替嵌套条件表达式：函数中的条件逻辑难以让人看清正常的执行路径，使用卫语句表现特殊情况，如下图所示，很明显逻辑清晰很多

![image.png](image.png)

以多态取代条件表达式：如果有条件表达式，根据对象类型不同选择不同行为，可以将条件表达式的每个分支放进一个子类的复写函数中，把原始的函数声明为抽象函数

引入Null对象：如果需要检查一个对象是不是null，可以把null值改为null对象，好处是不需要去询问是什么类型，而是直接调用对应方法即可

引入断言：某一段代码需要对程序的状态做出某种假设，可以使用断言明确表现这个假设。opencv中就有很多对于输入数据的断言，好处就是如果输入数据不是按照要求输出，直接报错避免程序后续执行很多再出问题不方便定位

## 3.5简化函数调用

函数改名：函数的名称未能揭示函数的用途，修改函数的名字。这是工程化编码最基本的规范了。比如我经常开发的C++，基本就使用Google代码规范写，因为有很多现成的工具可以直接集成到ci中

添加参数：某函数需要从调用端获得更多信息，为此函数需要添加一个对象参数，让该对象带进函数的所需信息。有的代码不给函数加参数，然后然后传参环节调用数据，尤其使用python这种动态类型语言，项目一大非常难以维护

移除参数：如果函数根本就不用某个参数，那么就把这个参数移除

将查询函数与修改函数分离：如果某个函数即返回对象状态值又修改对象状态，可以把这个函数拆分出查询函数和设置函数。在进行类设计的时候，接口尽量只做专一的一件事情

引入参数对象：某些参数总是很自然同时出现，以一个对象取代这些参数

隐藏函数：某个函数从来没有被其他类使用过，可以将这个函数修改为private

用工厂函数取代构造函数：如果创建对象时不仅仅是做简单的构建动作，将构建动作替换为工厂函数。工厂函数可以根据参数的配置来构建不同的类型，提供比构造函数更丰富的功能

用测试取代异常：面对一个调用者可以预先检查的条件，可以直接检查条件，进而进行处理，没必要什么都抛异常。异常主要用于异常罕见的行为，对于在预期内的行为直接处理

## 3.6处理概括(继承)关系

这部分都是面向对象编程中非常常见的用法

字段上移：两个子类拥有相同的字段，将该字段移至超类中

函数上移：两个子类拥有相同的函数，把函数移动到超类中

构造函数本体上移：子类中拥有一些构造函数，它们的本体几乎一致，在超类中新建一个构造函数，并在子类构造函数中调用

函数下移：超类中某个函数只与部分子类有关，将这个函数移动到相关的子类中

字段下移：超类中某个字段只被部分子类用到，把这个字段移动到对应子类中去

提炼子类：类中某些特性只被某个子类用到，新建一个子类，把这部分代码转移到子类中去

提炼超类：两个类有相似特性，为两个类建立超类，将相同特性移至超类中

提炼接口：若干用户使用类接口的同一子集，或者两个类的接口有部分相同，将相同的子集提炼到独立接口中

折叠继承体系：超类和子类没有太大区别，可以把它们合为一体

塑造模板函数：有一些子类，其中相应的某些函数以相同的顺序执行类似的操作，但是各自操作的细节有所不同，可以把这些操作分别放进独立函数，并保持它们相同的签名，于是原函数也就变得相同了，然后将原函数移到超类中。看下面的图就比较好理解了

![image.png](image%201.png)

以委托取代继承：某个子类只使用超类接口中的一部分，或者根本不需要使用继承而来的数据，在子类中新建一个字段来保存超类，调整子类函数，令其改而委托超类，并去掉二者之间的继承关系。本质上就是继承关系没啥用，直接调用原来的超类当做成员就完事了

## 3.7大型重构

如果代码量小，其实重构多带来的收益不会很大。只有对大型软件进行重构，且使用了合理的手段实现了重构目标才会凸显出重构的价值。大型软件的重构到了类级别或者代码级，使用的方法就是前面那些。除此之外会有一些非纯技术的问题也是很核心的，首先就是进行大规模重构前，要在团队中形成共识。安排时间人员资源进行这个事情。而且大规模的重构不会很快就显现出来效果，这对于团队的信任非常值得考验

梳理并分解继承体系：某个继承体系同时承担两项责任，可以建立两个继承体系，通过委托关系让其中一个可以调用另外一个

将过程化的设计转为面向对象的设计：过程化代码即使使用C开发，一旦项目大了也要使用一些OO的思想去设计代码，所以对于这种过程化的代码，进行抽象分散到类中维护比较方便，也更易于拓展

将领域的表述和显示分离：在架构设计上，把前端GUI显示和后端业务逻辑进行分离。在同一个进程内可以使用MVC设计模式，夸进程就更多了比如用websocket，共享内存等等方法隔离前端和后端

提炼继承体系：某个类做了太多工作，其中一部分是以大量条件表法师完成的，可以建立继承体系，用子类表示一种特殊情况

# 4.重构工具

这本书写的时候已经很久远了，对工具的描述不太准确了。所以我就自己写一些吧

首先编程语言和ide经过这么多年的发展，当前的主流ide都是自带一些重构工具的。这部分相对比较完善了。这些工具基本都是使用前面说的一些基础思想去做优化并且开发一些工具集成进入ide中。这部分理论内容对于绝大多数开发者并不太关注，真正研究这块的都是设计编程语言及其编译器工具链的团队，ide公司才会考虑。比如JetBrains，微软，谷歌，华为等。

软件重构既包含理论也包含实践，本质上还是工程驱动。学术上的研究主要可以参考会议 OOPSLA(Object-Oriented Programming, Systems, Languages & Applications)，这个会议在前些年被整合到新的会议中去了SPLASH(Systems, Programming, Languages, and Applications: Software for Humanity),这个会主要就专注于编程语言和配套的软件工程，还算比较权威，

这是去年这个会议地址：

[https://2025.splashcon.org/](https://2025.splashcon.org/)

下面是论文截图

![image.png](image%202.png)

实际上比较新的论文很多都在做基于AI的软件工程理论算法或者工具开发。在LLM出来之前，即使是谷歌自动驾驶这种对于软件质量体系要求比价高的团队，基本也是使用llvm的工具链集成到CI/CD中做代码检测，比如clang-tidy。llvm的工具链都是基于编译器那套抽象语法树上做的各种算法分析。好处是非常精准，问题是用起来对于不同场景泛化能力差，使用不方便。甚至对于一些代码检测工具，还经常出现误报问题。

我在文章开头说的目前AI浪潮，尤其对于当前软件体系的变革，必然大量使用agent来构建新一代的软件开发测试部署运维体系。一个突出的特点就是要摆脱之前的工具泛化困难问题，可以更加柔性甚至低门槛的适配不同的软件工程项目需求。打造更灵活高效的软件开发体系。

还有一个问题就是当前不论是claude code还是codex，其自带的agent依然不能说完全满足工程化软件开发的要求。当我们自己开发agent去打造世界第一流水准的软件开发团队时，这个会议的论文是有一定参考性的。

比如截图的第一篇论文

**ABC: Towards a Universal Code Styler through Model Merging**

下图是论文中生成代码风格差异对比

![image.png](image%203.png)

这篇论文是用LLM做代码风格转换。我们在使用AI编程进行团队开发的时候，一定是会要求整体的代码满足某一代码规范。比如我开发C++应用程序就要求团队使用google代码规范。我平时是自己写了SKILL，启动的时候就让AI记忆代码规范要求。这样AI生成的代码可以按照我要求的规范去写。但是这样做也不完美，一个问题是对话存在上下文限制，不能无限的聊下去，内容多了之后，会忘记之前的内容。另外就是SKILL如果写的非常详细，代码生成的慢，SKILL如果写的比较省略，代码生成的质量有不足。所以生成的代码依然要进行风格转换。我对比过codex，claude code 和gemini，效果各异。尤其codex的转换还有很大提升空间。

我前面举例的论文，自己训练了模型，不是只改agent。对于我这种没办法改变基础模型的人，指导意义不大，但是对于codex。cluade，或者国内智普，kimi团队，就有一定参考意义了。